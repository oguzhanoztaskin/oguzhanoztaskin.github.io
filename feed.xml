<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://oguzhanstech.com/feed.xml" rel="self" type="application/atom+xml" /><link href="https://oguzhanstech.com/" rel="alternate" type="text/html" /><updated>2026-08-05T18:35:40+00:00</updated><id>https://oguzhanstech.com/feed.xml</id><title type="html">Oguzhan’s Tech</title><subtitle>A blog about my hustles, which are mostly about cryptography, cyber security, or software projects. I fill in my posts/projects with the help of LLMs.</subtitle><entry><title type="html">A Zero-Knowledge Sudoku Prover</title><link href="https://oguzhanstech.com/2026/07/21/zk-sudoku-prover.html" rel="alternate" type="text/html" title="A Zero-Knowledge Sudoku Prover" /><published>2026-07-21T00:00:00+00:00</published><updated>2026-07-21T00:00:00+00:00</updated><id>https://oguzhanstech.com/2026/07/21/zk-sudoku-prover</id><content type="html" xml:base="https://oguzhanstech.com/2026/07/21/zk-sudoku-prover.html"><![CDATA[<p>In the previous posts we built up some machinery: the <a href="/2025/10/13/kzg-poly-commit-scheme.html">KZG Polynomial Commitment Scheme</a> for committing to a secret polynomial and proving its evaluations, and <a href="/2025/11/07/kzg-batching-in-plonk.html">KZG Batching in Plonk</a> for turning “this polynomial is zero on the whole domain $H$” into a single quotient check. I promised these would pay off in a later post where we do something more complicated. This is that post.</p>

<p>The something is a real, working zero-knowledge prover for Sudoku: <em>I know a valid completion of this puzzle, and I won’t show it to you.</em> I built a complete polynomial-IOP argument system over BN254 in Rust (arkworks), blinded for zero knowledge, made non-interactive with Fiat–Shamir, and compiled the whole thing to WebAssembly so you can <a href="#try-it">play with it in your browser</a> at the end of this post.</p>

<p><strong>Disclaimer:</strong> These are my personal notes and understanding of the topic. They may contain errors or oversimplifications. I don’t claim to provide absolute truth — rather, I invite discussion and corrections. If you spot any mistakes or have insights to share, please reach out!</p>

<h2 id="sudoku-is-a-pile-of-multiset-constraints">Sudoku is a pile of multiset constraints</h2>

<p>Strip Sudoku down to its logic. An $n \times n$ grid (with $n = k^2$, so $k=3$ is ordinary $9\times9$) is a witness: one function from $n^2$ cells to values $\lbrace 1,\dots,n \rbrace$. The rules are $3n$ constraints — $n$ rows, $n$ columns, $n$ boxes — and every single one of them asserts <em>the same thing</em>:</p>

<blockquote>
  <p>these $n$ cells contain each of $1,\dots,n$ exactly once.</p>
</blockquote>

<p>That is a <strong>multiset equality</strong>: the multiset of values in the group equals $\lbrace 1,2,\dots,n \rbrace$. Nothing about <em>where</em> a value sits inside the group matters, only <em>which</em> values are present and how many times. This is the structure we want to exploit.</p>

<h2 id="naive-attempt-sums-and-products">Naive attempt: sums and products</h2>

<p>Here is the first design that comes to mind, and it’s wrong. A group holds $\lbrace 1,\dots,n \rbrace$ iff its values sum to $1+2+\dots+n$ and multiply to $n!$, right? So just constrain the sum and the product of each row, column, and box. Clean, cheap, no fancy arguments.</p>

<p>It’s unsound. Consider this multiset for $n=9$:</p>

\[\lbrace 1,2,4,4,4,5,7,9,9 \rbrace\]

<p>Its sum is $45$ and its product is $362{,}880 = 9!$ — both statistics match $\lbrace 1,\dots,9 \rbrace$ exactly — yet it has three 4’s and no 3, 6, or 8. Even if you <em>also</em> range-constrain every cell to $\lbrace 1,\dots,9 \rbrace$, a cheating prover can still slip this past you.</p>

<p>The fix is to not check <em>fixed</em> statistics but a <em>randomized</em> one.</p>

<h2 id="the-multiset-check-we-actually-want">The multiset check we actually want</h2>

<p>Two multisets $A = \lbrace a_j \rbrace$ and $B = \lbrace b_j \rbrace$ of equal size are equal if and only if the polynomials $\prod_j (X + a_j)$ and $\prod_j (X + b_j)$ are identical. By Schwartz–Zippel <a class="citation" href="#schwartz1980fast">(Schwartz, 1980; Zippel, 1979)</a>, if we evaluate both at a <em>random</em> $\gamma$ drawn after the prover has committed, they agree only with probability $n/\lvert\mathbb{F}\rvert$ unless the multisets are truly equal. The spoof above dies here: as a polynomial in $\gamma$, $\prod (\gamma + a_j)$ for that bad multiset simply is not $\prod_{i=1}^{9}(\gamma + i)$.</p>

<p>So for each group $g$ we want to check</p>

\[\prod_{j \in g} \big(f(\mathrm{cell}_j) + \gamma\big) \;=\; \prod_{i=1}^{n} (i + \gamma).\]

<p>The right-hand side is the same for every group and depends only on public data — call it $P(\gamma)$. The verifier can compute it itself in $n$ multiplications.</p>

<p>How do we prove a <em>product over a group</em> inside a polynomial IOP? With a running product, exactly the grand-product accumulator $Z$ from Plonk’s permutation argument <a class="citation" href="#gabizon2019plonk">(Gabizon et al., 2019)</a>. We start $Z=1$ at the beginning of a group and multiply in one factor $(f+\gamma)$ per step, so that at the end of the group $Z$ equals the accumulated product. Then we check the accumulated product equals $P(\gamma)$.</p>

<h2 id="one-commitment-read-many-ways">One commitment, read many ways</h2>

<p>I commit the whole solution <strong>exactly once</strong>, as a single polynomial $f$. Let $p = 2^{\lceil \log_2 n \rceil}$ be the “pitch” (16 for $9\times9$), and put cell $(r,c)$ at domain index $p \cdot r + c$:</p>

\[f(\omega^{\,pr+c}) = \text{value of cell } (r,c), \qquad 0 \le r,c &lt; n,\]

<p>with the leftover padding slots set to zero. The domain $H = \langle \omega \rangle$ has size $N = p^2$.</p>

<p>Now the trick: rows, columns, and boxes are all traversals of this <em>same</em> commitment, each with its own multiplicative step. Advancing the argument $x \mapsto \sigma x$ moves us to a neighbouring cell:</p>

<ul>
  <li><strong>rows</strong> step by $\omega$ (stride 1),</li>
  <li><strong>columns</strong> step by $\omega^{p}$ (stride $p$),</li>
  <li><strong>boxes</strong> step by $\omega$ inside a box-row and by $\omega^{\,p-k+1}$ when wrapping to the next box-row.</li>
</ul>

<p>Because a cell in row $r$ is read by the row traversal <em>and</em> by a column traversal <em>and</em> by a box traversal — all against the identical committed $f$ at the identical domain point — cross-constraint consistency comes for free. There are no copy constraints, no wiring permutation, no duplicated witness columns. If a cheater swaps two values within a row (preserving that row’s multiset), the column constraints reading the same commitment catch it. This is a nice payoff from committing once.</p>

<p>Each family gets some public <strong>selector</strong> polynomials, fixed at setup from the grid geometry alone: $q_{\mathsf{start}}$ marks where each group begins, $q_{\mathsf{step}}$ marks the traversal steps, $q_{\mathsf{end}}$ marks group ends.</p>

<h2 id="the-hidden-shuffle-argument">The hidden shuffle argument</h2>

<p>Here is the whole idea in one sentence: <strong>take Plonk’s permutation argument and delete the coordinate binding.</strong></p>

<p>Plonk’s permutation product accumulates factors of the form $(w_i + \beta\,\sigma(i) + \gamma)$, where the $\beta\,\sigma(i)$ term ties each value to <em>its position</em> — that’s what makes it prove a <em>permutation</em> rather than just a <em>multiset</em>. But Sudoku doesn’t care about position; it only cares about the multiset. So set $\beta = 0$. The coordinate binding vanishes and the argument degenerates — usefully — into exactly the multiset check we derived above.</p>

<p>One thing falls out of this, and it is what makes the argument so cheap: the right-hand side $P(\gamma) = \prod_{i=1}^n (i+\gamma)$ is a <strong>public scalar</strong>. Plonk’s permutation argument compares two witness-dependent products against each other; here the target multiset is fixed and public, so that entire side collapses to one number the verifier computes for itself. Nothing has to be committed for it.</p>

<p>Each family carries one accumulator $Z$, and we enforce, for all $x \in H$:</p>

\[\begin{align}
q_{\mathsf{start}}(x)\,\big(Z(x) - 1\big) &amp;= 0, \notag \\
q_{\mathsf{step}}(x)\,\big(Z(\sigma x) - Z(x)(f(x) + \gamma)\big) &amp;= 0, \notag \\
q_{\mathsf{end}}(x)\,\big(Z(x)(f(x) + \gamma) - P(\gamma)\big) &amp;= 0. \notag
\end{align}\]

<p>The first line pins the accumulator to $1$ at group starts, the second multiplies in one cell per step, the third checks the finished product hits the public target. These are polynomial identities that must hold on $H$ — precisely the “zero on the whole domain” situation the <a href="/2025/11/07/kzg-batching-in-plonk.html">batching post</a> taught us to fold into a single quotient. We aggregate all of them (across the three families) with powers of a challenge $\alpha$, divide by $Z_H$, commit the quotient, and open everything at a random $\zeta$ with batched KZG — <a href="#one-equation">written out in full below</a>.</p>

<p>The result is a constant-size proof — <strong>544 bytes</strong>, independent of grid size — of nine group elements and eight field elements.</p>

<h2 id="binding-the-proof-to-this-puzzle">Binding the proof to <em>this</em> puzzle</h2>

<p>Everything so far proves “$f$ is <em>some</em> valid grid.” A puzzle prover has to prove a valid completion of <em>the specific puzzle on screen</em>. The revealed clues are public input, and I bind them the Plonk public-input way. Let $S = \lbrace (i, v_i) \rbrace$ be the clue cells and define two public polynomials</p>

\[q_{\mathsf{clue}}(X) = \sum_{i \in S} L_i(X), \qquad c(X) = \sum_{i \in S} v_i\, L_i(X),\]

<p>then add one more constraint to the aggregation:</p>

\[q_{\mathsf{clue}}(X)\,\big(f(X) - c(X)\big) \equiv 0 \pmod{Z_H(X)}.\]

<p>On a clue cell this forces $f = v_i$; off the clue cells $q_{\mathsf{clue}}$ is zero and it says nothing. What I like about it is the cost: it only touches $f(\zeta)$, which we already open, so it adds <strong>no</strong> commitment, <strong>no</strong> opening, and <strong>no</strong> blinder — proof sizes don’t move. The verifier never takes $q_{\mathsf{clue}}(\zeta)$ or $c(\zeta)$ from the prover; it computes both itself in $O(\lvert S\rvert)$ from the barycentric form</p>

\[L_i(\zeta) = \frac{\omega^{i}}{N} \cdot \frac{\zeta^{N} - 1}{\zeta - \omega^{i}}\]

<p>with one batched inversion. And because the clue set is absorbed into the Fiat–Shamir transcript <em>before</em> any challenge is drawn, the whole proof is cryptographically tied to the instance: an honest proof for one puzzle fails against any other, even a different puzzle the same solution happens to satisfy. Proofs are not replayable across instances.</p>

<h2 id="one-equation">Everything, in one equation</h2>

<p>We now have all the pieces, so let’s write down what is actually being proved. Put $\bar f(X) = f(X) + \gamma$, and for a family $\mathcal{F}$ with step shifts $\sigma_0,\dots,\sigma_{m-1}$ collect that family’s constraints into</p>

\[\begin{align}
C_{\mathcal{F}}(X) \;=\;\;&amp; q^{\mathcal{F}}_{\mathsf{start}}(X)\,\big(Z_{\mathcal{F}}(X) - 1\big) \notag \\
+\;\;&amp; \sum_{v=0}^{m-1} \alpha^{\,v+1}\, q^{\mathcal{F}}_{\mathsf{step},v}(X)\,\big(Z_{\mathcal{F}}(\sigma_v X) - Z_{\mathcal{F}}(X)\,\bar f(X)\big) \notag \\
+\;\;&amp; \alpha^{\,m+1}\, q^{\mathcal{F}}_{\mathsf{end}}(X)\,\big(Z_{\mathcal{F}}(X)\,\bar f(X) - P(\gamma)\big). \notag
\end{align}\]

<p>Rows step only by $\sigma_0 = \omega$ and columns only by $\sigma_0 = \omega^{p}$, so each contributes three constraints; boxes step by both $\sigma_0 = \omega$ and $\sigma_1 = \omega^{\,p-k+1}$ and contribute four. Stack the three families and the clue constraint with further powers of $\alpha$, and the entire argument is the single claim that</p>

\[C_{\mathsf{row}}(X) + \alpha^{3}\,C_{\mathsf{col}}(X) + \alpha^{6}\,C_{\mathsf{box}}(X) + \alpha^{10}\,q_{\mathsf{clue}}(X)\big(f(X) - c(X)\big) \;=\; q(X)\,Z_H(X).\]

<p>That is the whole prover: eleven constraints, one identity, one quotient. Because $\alpha$ is drawn only after $f$ and the three accumulators are committed, a prover who violates any one of the eleven cannot hope for the aggregation to cancel it — that happens with probability $O(1)/\lvert\mathbb{F}\rvert$ over $\alpha$. Divisibility by $Z_H$ is the “zero on the whole domain” claim from the batching post, so it is checked at a single random point $\zeta$:</p>

\[C_{\mathsf{row}}(\zeta) + \alpha^{3}\,C_{\mathsf{col}}(\zeta) + \alpha^{6}\,C_{\mathsf{box}}(\zeta) + \alpha^{10}\,q_{\mathsf{clue}}(\zeta)\big(f(\zeta) - c(\zeta)\big) \stackrel{?}{=} q(\zeta)\,\big(\zeta^{N} - 1\big).\]

<p>Notice how little of this the verifier takes on faith. Every selector, the target $P(\gamma)$, the clue polynomials $q_{\mathsf{clue}}(\zeta)$ and $c(\zeta)$, and $Z_H(\zeta) = \zeta^{N}-1$ it computes itself from public data. It does not even need to be told $q(\zeta)$: the equation above <em>determines</em> it, so the verifier divides rather than asking. That leaves eight field elements coming from the prover:</p>

\[f(\zeta),\;\; Z_{\mathsf{row}}(\zeta),\;\; Z_{\mathsf{col}}(\zeta),\;\; Z_{\mathsf{box}}(\zeta),\;\; Z_{\mathsf{row}}(\omega\zeta),\;\; Z_{\mathsf{box}}(\omega\zeta),\;\; Z_{\mathsf{col}}(\omega^{p}\zeta),\;\; Z_{\mathsf{box}}(\omega^{\,p-k+1}\zeta).\]

<p>And nothing so far forces those eight numbers to be honest — that is what the commitments are for. The evaluations fall into four groups by the point they are opened at — $f$, the three accumulators and the quotient at $\zeta$; the row and column accumulators once more at their own shifted point; the box accumulator at both of its shifted points — so the prover sends one witness per point: for point $z$ opening polynomials $p_0, p_1, \dots$, it commits the witness of their random linear combination in powers of a final challenge $u$, and the verifier checks</p>

\[e\Big(\textstyle\sum_i u^{i}\,[p_i(x)]_ 1 - \big(\textstyle\sum_i u^{i}\,p_i(z)\big)[1]_ 1,\;\; [1]_ 2\Big) \stackrel{?}{=} e\big([W_z(x)]_ 1,\;\; [x]_ 2 - z[1]_ 2\big).\]

<p>This is exactly the pairing check from the batching post, with the left-hand commitment now assembled by the verifier out of the commitments it was given and the evaluations it was told. It holds only if that combination genuinely vanishes at $z$ — and since $u$ is drawn after the evaluations are fixed, that forces <em>every</em> claimed evaluation in the group to be correct. Four points, four witnesses — but not four pairing checks: every instance pairs against the same two fixed $\mathbb{G}_ 2$ elements, so one more challenge folds all four into a single two-term pairing product. Those four witnesses plus the five commitments are precisely the nine group elements of the 544-byte proof. Exactly which polynomial is committed when, opened where, and which $p_i$ enters which of the four checks is pinned down in the <a href="#protocol">full specification below</a>.</p>

<h2 id="protocol">The protocol, in full</h2>

<p>Everything above with nothing left implicit — enough to reimplement. $\mathbb{F}$ is the BN254 scalar field, $\mathbb{G}_ 1,\mathbb{G}_ 2$ its pairing groups, $[y]_ 1$ a commitment, $x$ the SRS secret.</p>

<h3 id="0-public-parameters">0. Public parameters</h3>

\[k = \sqrt{n},\qquad
p = 2^{\lceil \log_2 n\rceil},\qquad
N = p^{2},\qquad
H = \langle\omega\rangle,\ \lvert H\rvert = N,\qquad
Z_H(X) = X^{N}-1\]

\[K = \lbrace g\,\nu^{\,j}\rbrace_{j&lt;4N},\quad \nu \text{ of order } 4N,\quad g \text{ a generator of } \mathbb{F}^{\times},\quad\text{so } K \cap H = \varnothing\]

\[\mathrm{SRS} = \big(\lbrace [x^{i}]_ 1\rbrace_{i=0}^{4N},\; [1]_ 2,\; [x]_ 2\big)\]

<p>Cell $(r,c)$ sits at domain index $\mathrm{idx}(r,c) = pr + c$. The three families, as index sets in traversal order, for $0 \le j &lt; n$:</p>

\[\begin{align}
\mathcal{G}^{\mathsf{row}}_{r}[j] &amp;= pr + j, \qquad &amp;\sigma^{\mathsf{row}}_{0} &amp;= \omega \notag \\
\mathcal{G}^{\mathsf{col}}_{c}[j] &amp;= pj + c, \qquad &amp;\sigma^{\mathsf{col}}_{0} &amp;= \omega^{p} \notag \\
\mathcal{G}^{\mathsf{box}}_{b}[j] &amp;= p\big(k\lfloor b/k\rfloor + \lfloor j/k\rfloor\big) + k\,(b \bmod k) + (j \bmod k), \qquad &amp;\sigma^{\mathsf{box}}_{0} &amp;= \omega,\ \ \sigma^{\mathsf{box}}_{1} = \omega^{\,p-k+1} \notag
\end{align}\]

<p>Rows and columns always take variant $0$; boxes take variant $v(j) = 1$ when $j \bmod k = k-1$ (the box-row wrap) and $0$ otherwise. Selectors are the $H$-interpolants of the indicator vectors, over all groups $\mathcal{G}$ of the family, zero everywhere else:</p>

\[q^{\mathcal{F}}_{\mathsf{start}}\big(\omega^{\mathcal{G}[0]}\big) = 1,
\qquad
q^{\mathcal{F}}_{\mathsf{step},v}\big(\omega^{\mathcal{G}[j]}\big) = 1 \ \ (j &lt; n-1,\ v(j) = v),
\qquad
q^{\mathcal{F}}_{\mathsf{end}}\big(\omega^{\mathcal{G}[n-1]}\big) = 1\]

<p>Instance-dependent, both sides: with $S = \lbrace (i, v_i)\rbrace$ the clues at domain indices,</p>

\[P(\gamma) = \prod_{i=1}^{n}(i+\gamma),
\qquad
q_{\mathsf{clue}} = \sum_{i \in S} L_i,
\qquad
c = \sum_{i \in S} v_i L_i\]

<h3 id="1-transcript">1. Transcript</h3>

<p>SHA-256 over a running state $\mathrm{st}$, initialised with the domain separator <code class="language-plaintext highlighter-rouge">"sudoku-shuffle"</code>:</p>

\[\begin{align}
\mathsf{absorb}(\ell, m) &amp;:\quad \mathrm{st} \mathrel{\Vert}= \ell \mathrel{\Vert} m \notag \\
\mathsf{challenge}(\ell) &amp;:\quad \mathrm{st} \mathrel{\Vert}= \ell; \quad d \leftarrow \mathrm{SHA256}(\mathrm{st}); \quad \mathrm{st} \mathrel{\Vert}= d; \quad \textbf{return } \mathrm{Fr}_{\mathrm{le}}(d) \bmod r \notag
\end{align}\]

<p>Group elements are absorbed compressed (32 B), scalars little-endian (32 B), integers as <code class="language-plaintext highlighter-rouge">u64</code> little-endian. The order is fixed and total:</p>

<table>
  <thead>
    <tr>
      <th>#</th>
      <th>operation</th>
      <th>label</th>
      <th>data</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>absorb</td>
      <td><code class="language-plaintext highlighter-rouge">"n"</code></td>
      <td>$n$</td>
    </tr>
    <tr>
      <td>2</td>
      <td>absorb</td>
      <td><code class="language-plaintext highlighter-rouge">"puzzle"</code></td>
      <td>all $n^2$ clue cells, $0$ for hidden</td>
    </tr>
    <tr>
      <td>3</td>
      <td>absorb</td>
      <td><code class="language-plaintext highlighter-rouge">"f"</code></td>
      <td>$[f(x)]_ 1$</td>
    </tr>
    <tr>
      <td>4</td>
      <td><strong>challenge</strong></td>
      <td><code class="language-plaintext highlighter-rouge">"gamma"</code></td>
      <td>$\rightarrow \gamma$</td>
    </tr>
    <tr>
      <td>5</td>
      <td>absorb</td>
      <td><code class="language-plaintext highlighter-rouge">"zr"</code>, <code class="language-plaintext highlighter-rouge">"zc"</code>, <code class="language-plaintext highlighter-rouge">"zb"</code></td>
      <td>$[Z_{\mathsf{row}}(x)]_ 1$, $[Z_{\mathsf{col}}(x)]_ 1$, $[Z_{\mathsf{box}}(x)]_ 1$</td>
    </tr>
    <tr>
      <td>6</td>
      <td><strong>challenge</strong></td>
      <td><code class="language-plaintext highlighter-rouge">"alpha"</code></td>
      <td>$\rightarrow \alpha$</td>
    </tr>
    <tr>
      <td>7</td>
      <td>absorb</td>
      <td><code class="language-plaintext highlighter-rouge">"q"</code></td>
      <td>$[q(x)]_ 1$</td>
    </tr>
    <tr>
      <td>8</td>
      <td><strong>challenge</strong></td>
      <td><code class="language-plaintext highlighter-rouge">"zeta"</code></td>
      <td>$\rightarrow \zeta$</td>
    </tr>
    <tr>
      <td>9</td>
      <td>absorb</td>
      <td><code class="language-plaintext highlighter-rouge">"f_z"</code>, <code class="language-plaintext highlighter-rouge">"zr_z"</code>, <code class="language-plaintext highlighter-rouge">"zc_z"</code>, <code class="language-plaintext highlighter-rouge">"zb_z"</code>, <code class="language-plaintext highlighter-rouge">"q_z"</code>, <code class="language-plaintext highlighter-rouge">"zr_w"</code>, <code class="language-plaintext highlighter-rouge">"zb_w"</code>, <code class="language-plaintext highlighter-rouge">"zc_wp"</code>, <code class="language-plaintext highlighter-rouge">"zb_wb"</code></td>
      <td>the nine evaluations, in that order — but <code class="language-plaintext highlighter-rouge">q_z</code> is <em>derived</em>, not sent (§3)</td>
    </tr>
    <tr>
      <td>10</td>
      <td><strong>challenge</strong></td>
      <td><code class="language-plaintext highlighter-rouge">"u"</code></td>
      <td>$\rightarrow u$ — batches openings at one point</td>
    </tr>
    <tr>
      <td>11</td>
      <td>absorb</td>
      <td><code class="language-plaintext highlighter-rouge">"w_z"</code>, <code class="language-plaintext highlighter-rouge">"w_w"</code>, <code class="language-plaintext highlighter-rouge">"w_wp"</code>, <code class="language-plaintext highlighter-rouge">"w_wb"</code></td>
      <td>the four opening witnesses</td>
    </tr>
    <tr>
      <td>12</td>
      <td><strong>challenge</strong></td>
      <td><code class="language-plaintext highlighter-rouge">"v"</code></td>
      <td>$\rightarrow v$ — batches across points</td>
    </tr>
  </tbody>
</table>

<p>Absorbing the puzzle at step 2 — before any challenge — is what binds the proof to the instance.</p>

<h3 id="2-prover">2. Prover</h3>

<p><strong>Round 1.</strong> With $\mathbf{f}[\mathrm{idx}(r,c)] = \text{cell}(r,c)$ and $\mathbf{f} = 0$ on padding, and $b_i \leftarrow \mathbb{F}$ fresh:</p>

\[f(X) = \mathrm{IFFT}_ H(\mathbf{f}) + \Big(\sum_{i=0}^{1} b_i X^{i}\Big) Z_H(X),
\qquad
[f(x)]_ 1 = \sum_{i} f_i\,[x^{i}]_ 1\]

<p><strong>Round 2.</strong> Per family, per group, walking the traversal ($\mathbf{z} = 1$ off-group):</p>

\[\mathbf{z}\big[\mathcal{G}[0]\big] = 1,
\qquad
\mathbf{z}\big[\mathcal{G}[j+1]\big] = \mathbf{z}\big[\mathcal{G}[j]\big]\cdot\big(\mathbf{f}\big[\mathcal{G}[j]\big] + \gamma\big)\]

\[Z_{\mathcal{F}}(X) = \mathrm{IFFT}_ H(\mathbf{z}) + \Big(\sum_{i} b_i X^{i}\Big) Z_H(X)\]

<p>with the blinder running over $i &lt; 3$ for rows and columns and $i &lt; 4$ for boxes — always one more than the number of points that polynomial gets opened at.</p>

<p><strong>Round 3.</strong></p>

\[q(X) = \frac{C_{\mathsf{row}}(X) + \alpha^{3}C_{\mathsf{col}}(X) + \alpha^{6}C_{\mathsf{box}}(X) + \alpha^{10}\,q_{\mathsf{clue}}(X)\big(f(X)-c(X)\big)}{Z_H(X)},
\qquad \deg q \approx 2N\]

<p>computed in evaluation form on the coset $K$:<sup id="fnref:coset"><a href="#fn:coset" class="footnote" rel="footnote" role="doc-noteref">1</a></sup> one size-$4N$ coset FFT each for $f$, the three $Z_{\mathcal{F}}$, the shifted $Z_{\mathcal{F}}(\sigma_v X)$ (scale coefficient $i$ by $\sigma_v^{\,i}$), the selectors, $q_{\mathsf{clue}}$ and $c$; combine pointwise; scale by $Z_H^{-1}$, which is four precomputed values indexed by $j \bmod 4$;<sup id="fnref:zh"><a href="#fn:zh" class="footnote" rel="footnote" role="doc-noteref">2</a></sup> inverse coset FFT back to coefficients.</p>

<p><strong>Round 4.</strong> Eight evaluations — everything but $q(\zeta)$, which the verifier reconstructs:</p>

\[f(\zeta),\;\; Z_{\mathsf{row}}(\zeta),\;\; Z_{\mathsf{col}}(\zeta),\;\; Z_{\mathsf{box}}(\zeta),\;\; Z_{\mathsf{row}}(\omega\zeta),\;\; Z_{\mathsf{box}}(\omega\zeta),\;\; Z_{\mathsf{col}}(\omega^{p}\zeta),\;\; Z_{\mathsf{box}}(\omega^{\,p-k+1}\zeta)\]

<p>The transcript still absorbs $q(\zeta)$ in position five; both sides have it, one by evaluation and one by division.</p>

<p><strong>Round 5.</strong> One witness per distinct opening point, batching everything opened there with powers of $u$:</p>

\[W_z(X) = \frac{\sum_i u^{i}\,p^{(z)}_i(X) \;-\; \sum_i u^{i}\,p^{(z)}_i(z)}{X - z}\]

\[\begin{align}
p^{(\zeta)} &amp;= \big(f,\; Z_{\mathsf{row}},\; Z_{\mathsf{col}},\; Z_{\mathsf{box}},\; q\big) \notag \\
p^{(\omega\zeta)} &amp;= \big(Z_{\mathsf{row}},\; Z_{\mathsf{box}}\big) \notag \\
p^{(\omega^{p}\zeta)} &amp;= \big(Z_{\mathsf{col}}\big) \notag \\
p^{(\omega^{\,p-k+1}\zeta)} &amp;= \big(Z_{\mathsf{box}}\big) \notag
\end{align}\]

<p>The four witnesses then go into the transcript and yield the last challenge $v$, which exists purely to fold the four opening checks into one.</p>

<p>The proof is exactly this, in this order on the wire:</p>

\[\begin{align}
\pi \;=\; \Big(\;
&amp; [f(x)]_1,\;\; [Z_{\mathsf{row}}(x)]_1,\;\; [Z_{\mathsf{col}}(x)]_1,\;\; [Z_{\mathsf{box}}(x)]_1,\;\; [q(x)]_1, &amp;&amp; \text{the five commitments} \notag \\[2pt]
&amp; f(\zeta),\;\; Z_{\mathsf{row}}(\zeta),\;\; Z_{\mathsf{col}}(\zeta),\;\; Z_{\mathsf{box}}(\zeta), &amp;&amp; p^{(\zeta)} \text{ at } \zeta,\ \text{except } q(\zeta) \notag \\[2pt]
&amp; Z_{\mathsf{row}}(\omega\zeta),\;\; Z_{\mathsf{box}}(\omega\zeta), &amp;&amp; p^{(\omega\zeta)} \text{ at } \omega\zeta \notag \\[2pt]
&amp; Z_{\mathsf{col}}(\omega^{p}\zeta), &amp;&amp; p^{(\omega^{p}\zeta)} \text{ at } \omega^{p}\zeta \notag \\[2pt]
&amp; Z_{\mathsf{box}}(\omega^{\,p-k+1}\zeta), &amp;&amp; p^{(\omega^{\,p-k+1}\zeta)} \text{ at } \omega^{\,p-k+1}\zeta \notag \\[2pt]
&amp; [W_{\zeta}(x)]_1,\;\; [W_{\omega\zeta}(x)]_1,\;\; [W_{\omega^{p}\zeta}(x)]_1,\;\; [W_{\omega^{\,p-k+1}\zeta}(x)]_1
\;\Big) &amp;&amp; \text{one } W_z \text{ per point} \notag
\end{align}\]

<p>Lines two through five list each $p^{(z)}$ evaluated at its own $z$, for the four points in turn — so the eight scalars are not an arbitrary count. They are the four tuple sizes $5 + 2 + 1 + 1$, less the one value the verifier can reconstruct for itself. Nine compressed points plus eight scalars is $9 \times 32 + 8 \times 32 = 544$ bytes.</p>

<p>Commitments first, evaluations next, witnesses last — the transcript order of §1 is <em>not</em> the serialization order, since $u$ cannot be drawn until the evaluations have been absorbed.</p>

<h3 id="3-verifier">3. Verifier</h3>

<p>Replay the transcript to recover $\gamma, \alpha, \zeta, u$, then two checks.</p>

<p><strong>Check 1 — reconstruct $q(\zeta)$.</strong> Evaluate the eleven selectors at $\zeta$, compute $P(\gamma)$ in $n$ multiplications, and get the clue values in $O(\lvert S\rvert)$ with one batched inversion:</p>

\[q_{\mathsf{clue}}(\zeta) = \sum_{i \in S} L_i(\zeta),
\qquad
c(\zeta) = \sum_{i \in S} v_i L_i(\zeta),
\qquad
L_i(\zeta) = \frac{\omega^{i}}{N}\cdot\frac{\zeta^{N}-1}{\zeta-\omega^{i}}\]

<p>Then with $\bar f = f(\zeta) + \gamma$, and every other quantity below taken verbatim from the proof:</p>

\[\begin{align}
q(\zeta)\,\big(\zeta^{N}-1\big) \;=\;\;
 &amp; q^{\mathsf{row}}_{\mathsf{start}}(\zeta)\big(Z_{\mathsf{row}}(\zeta) - 1\big) \notag \\
+\;&amp; \alpha\;\, q^{\mathsf{row}}_{\mathsf{step},0}(\zeta)\big(Z_{\mathsf{row}}(\omega\zeta) - Z_{\mathsf{row}}(\zeta)\,\bar f\big) \notag \\
+\;&amp; \alpha^{2} q^{\mathsf{row}}_{\mathsf{end}}(\zeta)\big(Z_{\mathsf{row}}(\zeta)\,\bar f - P(\gamma)\big) \notag \\
+\;&amp; \alpha^{3} q^{\mathsf{col}}_{\mathsf{start}}(\zeta)\big(Z_{\mathsf{col}}(\zeta) - 1\big) \notag \\
+\;&amp; \alpha^{4} q^{\mathsf{col}}_{\mathsf{step},0}(\zeta)\big(Z_{\mathsf{col}}(\omega^{p}\zeta) - Z_{\mathsf{col}}(\zeta)\,\bar f\big) \notag \\
+\;&amp; \alpha^{5} q^{\mathsf{col}}_{\mathsf{end}}(\zeta)\big(Z_{\mathsf{col}}(\zeta)\,\bar f - P(\gamma)\big) \notag \\
+\;&amp; \alpha^{6} q^{\mathsf{box}}_{\mathsf{start}}(\zeta)\big(Z_{\mathsf{box}}(\zeta) - 1\big) \notag \\
+\;&amp; \alpha^{7} q^{\mathsf{box}}_{\mathsf{step},0}(\zeta)\big(Z_{\mathsf{box}}(\omega\zeta) - Z_{\mathsf{box}}(\zeta)\,\bar f\big) \notag \\
+\;&amp; \alpha^{8} q^{\mathsf{box}}_{\mathsf{step},1}(\zeta)\big(Z_{\mathsf{box}}(\omega^{\,p-k+1}\zeta) - Z_{\mathsf{box}}(\zeta)\,\bar f\big) \notag \\
+\;&amp; \alpha^{9} q^{\mathsf{box}}_{\mathsf{end}}(\zeta)\big(Z_{\mathsf{box}}(\zeta)\,\bar f - P(\gamma)\big) \notag \\
+\;&amp; \alpha^{10} q_{\mathsf{clue}}(\zeta)\big(f(\zeta) - c(\zeta)\big) \notag
\end{align}\]

<p>Read left to right this is a <em>definition</em>, not a comparison: divide by $\zeta^{N}-1$ and you have $q(\zeta)$, the ninth evaluation, without the prover sending it. (If $\zeta^{N} = 1$, reject — but $\zeta \in H$ happens with probability $N/\lvert\mathbb{F}\rvert$.) All the force has moved into Check 2, which now has to establish that the <em>committed</em> $q$ really opens to this number.</p>

<p><strong>Check 2 — the openings.</strong> Clearing the denominator in the Round 5 definition of $W_z$, the openings are honest exactly when</p>

\[\sum_i u^{i}\,\big(p^{(z)}_ i(X) - p^{(z)}_ i(z)\big) \;=\; W_z(X)\,(X-z).\]

<p>The verifier holds none of the $p^{(z)}_ i$, but it does not need them. Commitment is linear and a scalar $v$ commits to $v[1]_ 1$, so the left side can be committed straight from the proof — commitments for the first term, claimed evaluations for the second — and the pairing tests the identity at the secret $x$:</p>

\[e\Big(\textstyle\sum_i u^{i}\big[p^{(z)}_i(x)\big]_1 - \big(\textstyle\sum_i u^{i} p^{(z)}_i(z)\big)[1]_1,\;\; [1]_2\Big)
\;\stackrel{?}{=}\;
e\big([W_z(x)]_1,\;\; [x]_2 - z\,[1]_2\big)\]

<p>At $z = \zeta$, whose tuple is all five committed polynomials, that reads:</p>

\[\begin{align}
e\Big(\;&amp; [f(x)]_1 + u\,[Z_{\mathsf{row}}(x)]_1 + u^{2}[Z_{\mathsf{col}}(x)]_1 + u^{3}[Z_{\mathsf{box}}(x)]_1 + u^{4}[q(x)]_1 \notag \\
&amp;-\big(f(\zeta) + u\,Z_{\mathsf{row}}(\zeta) + u^{2}Z_{\mathsf{col}}(\zeta) + u^{3}Z_{\mathsf{box}}(\zeta) + u^{4}q(\zeta)\big)[1]_1,\;\; [1]_2\Big) \notag \\
&amp;\stackrel{?}{=}\; e\big([W_{\zeta}(x)]_1,\;\; [x]_2 - \zeta\,[1]_2\big) \notag
\end{align}\]

<p>and at $z = \omega^{p}\zeta$, whose tuple is the single polynomial $Z_{\mathsf{col}}$, it degenerates to a plain KZG opening:</p>

\[e\big([Z_{\mathsf{col}}(x)]_1 - Z_{\mathsf{col}}(\omega^{p}\zeta)\,[1]_1,\;\; [1]_2\big)
\;\stackrel{?}{=}\;
e\big([W_{\omega^{p}\zeta}(x)]_1,\;\; [x]_2 - \omega^{p}\zeta\,[1]_2\big)\]

<p><strong>Fold the four into one.</strong> Taken literally that is four equations and eight pairings, and it is wasteful: the $\mathbb{G}_ 2$ arguments differ only by the scalar $z$. Split the right-hand side by bilinearity,</p>

\[e\big([W_z(x)]_1,\;\; [x]_2 - z\,[1]_2\big)
\;=\;
e\big([W_z(x)]_1,\;\; [x]_2\big)\cdot e\big({-z}\,[W_z(x)]_1,\;\; [1]_2\big),\]

<p>and move the second factor left. Writing $L_z$ for the $\mathbb{G}_ 1$ element on the left of the check above, every instance now pairs against the <em>same</em> two fixed $\mathbb{G}_ 2$ points:</p>

\[e\big(L_z + z\,[W_z(x)]_1,\;\; [1]_2\big) \;\stackrel{?}{=}\; e\big([W_z(x)]_1,\;\; [x]_2\big)\]

<p>so one random $v$ collapses all four of them into a single equation:</p>

\[e\Big(\textstyle\sum_{j} v^{\,j}\big(L_{z_j} + z_j\,[W_{z_j}(x)]_1\big),\;\; [1]_2\Big)
\;\stackrel{?}{=}\;
e\Big(\textstyle\sum_{j} v^{\,j}\,[W_{z_j}(x)]_1,\;\; [x]_2\Big)\]

<p>Two pairings for all four points, evaluated as one two-term product so they also share a single final exponentiation. The cost is about ten extra $\mathbb{G}_ 1$ scalar multiplications, perhaps a twentieth of one pairing. $v$ <strong>must</strong> be drawn after the witnesses are in the transcript — if the prover knew $v$ first it could fit witnesses to the combination without satisfying any individual check.</p>

<p>Accept iff that one equation holds.</p>

<h2 id="a-word-on-zero-knowledge">A word on zero knowledge</h2>

<p>To keep the hidden cells hidden, every witness-dependent polynomial carries Plonk-style blinders: a polynomial opened at $m$ points is committed as $p(X) + b(X) Z_H(X)$ with a random $b$ of degree $m$ — that is, $m+1$ random scalars. The blinders vanish on $H$, so the constraint semantics are untouched, but the off-domain openings the verifier sees become uniformly random and the whole view is simulatable.</p>

<p>That $m+1$ is where the budget goes, and it is worth doing the arithmetic against the eight evaluations the prover sends. Every one of them is an opening of a blinded polynomial: $f$ at one point, $Z_{\mathsf{row}}$ and $Z_{\mathsf{col}}$ at two each, $Z_{\mathsf{box}}$ at three. So the blinding cost is $2 + 3 + 3 + 4 = 12$ scalars — one more per polynomial than it has openings, because the $m$ revealed evaluations consume $m$ degrees of freedom and the last one keeps the commitment itself from being pinned down by them. The quotient needs no blinder: $q(\zeta)$ is never sent, and even the value the verifier reconstructs is a public function of the eight blinded ones, so it carries nothing they don’t already.</p>

<h2 id="how-fast-is-it">How fast is it?</h2>

<p>Criterion medians on my laptop, single-threaded:</p>

<table>
  <thead>
    <tr>
      <th>grid</th>
      <th>prove</th>
      <th>verify</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>9×9</td>
      <td>28.2 ms</td>
      <td>1.32 ms</td>
    </tr>
    <tr>
      <td>16×16</td>
      <td>28.2 ms</td>
      <td>1.34 ms</td>
    </tr>
    <tr>
      <td>25×25</td>
      <td>87.3 ms</td>
      <td>1.49 ms</td>
    </tr>
  </tbody>
</table>

<p>Proving stays under 100 ms even at $25\times25$, and verification is a flat ~1.4 ms regardless of grid size — the proof is a constant nine group elements and eight field elements no matter how big the puzzle. (The $9\times9$ and $16\times16$ prove times coincide because with the single-commitment layout they share the same domain $N=256$ — the delta is protocol machinery, not padding.)</p>

<p>Nearly all of that verify time is the two Miller loops of the batched opening check. The rest — eleven selector evaluations, the barycentric clue sum, a dozen $\mathbb{G}_ 1$ scalar multiplications — barely registers beside them, which is also why the number hardly moves with $n$. There is not much left to take without changing the layout, and the layout is what fixes the four opening points: rows, columns and boxes need three distinct traversal shifts, and no relabelling of the traversals collapses them into fewer.</p>

<h2 id="try-it">Try it</h2>

<p>The prover compiles to WebAssembly and runs entirely in your browser — pick a size, enter a puzzle (or let the built-in solver find a completion), generate a proof, verify it, and try tampering with the clues or the proof bytes to watch verification fail. It proves a $9\times9$ in well under a second client-side.</p>

<p><strong><a href="/assets/sudoku-demo.html">Open the demo →</a></strong></p>

<p>One honest caveat printed on the page itself: the demo seeds the KZG setup deterministically so proofs stay shareable across page loads, which makes that particular setup a forgeable toy — a real deployment would use a proper ceremony SRS.</p>

<h2 id="conclusion">Conclusion</h2>

<p>Sudoku is small, but the structure it exposes is not. “Many small, structured, exact-multiplicity constraints over one shared witness” is the shape of range decompositions, permutation networks, and state-machine transitions too. The lesson I take is that when the target multiset is a fixed little set like $\lbrace 1,\dots,n \rbrace$, you don’t need the full strength of a permutation argument: deleting the coordinate binding from Plonk’s permutation product — turning it into a hidden shuffle — collapses the right-hand side to a public scalar, and the single-commitment traversal layout and the zero-cost clue binding sit naturally on top.</p>

<h2 id="references">References</h2>

<ol class="bibliography"><li><span id="schwartz1980fast">Schwartz, J. T. (1980). Fast probabilistic algorithms for verification of polynomial identities. <i>Journal of the ACM (JACM)</i>, <i>27</i>(4), 701–717.</span></li>
<li><span id="zippel1979probabilistic">Zippel, R. (1979). Probabilistic algorithms for sparse polynomials. <i>International Symposium on Symbolic and Algebraic Manipulation</i>, 216–226.</span></li>
<li><span id="gabizon2019plonk">Gabizon, A., Williamson, Z. J., &amp; Ciobotaru, O. (2019). Plonk: Permutations over lagrange-bases for oecumenical noninteractive arguments of knowledge. <i>Cryptology EPrint Archive</i>.</span></li></ol>

<h2 id="footnotes">Footnotes</h2>

<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:coset">
      <p>The division cannot be done on $H$ itself: there $Z_H$ vanishes by construction and so does the numerator, so every point reads $0/0$. $K$ is a coset of a larger power-of-two subgroup — power-of-two so that radix-2 FFT still applies, and shifted by a generator $g$ of $\mathbb{F}^{\times}$ so no point of $K$ can land in $H$ (a generator lies in no proper subgroup, and $H \subseteq \langle\nu\rangle$). Size $4N$ is the smallest power of two above the numerator’s degree, which reaches about $3N$: selector $\times\, Z \times f$, all blinded. <a href="#fnref:coset" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:zh">
      <p>At the $j$-th point of $K$, $Z_H(g\nu^{\,j}) = (g\nu^{\,j})^{N} - 1 = g^{N}(\nu^{N})^{j} - 1$. Since $\nu$ has order $4N$, $\nu^{N}$ is a primitive 4th root of unity, so $(\nu^{N})^{j}$ depends only on $j \bmod 4$ and $Z_H$ takes just four distinct values across all $4N$ points. One batch inversion of those four replaces $4N$ field inversions — 4 instead of 1024 for a $9\times9$. <a href="#fnref:zh" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name></name></author><category term="cryptography" /><category term="zkp" /><category term="plonk" /><category term="sudoku" /><summary type="html"><![CDATA[In the previous posts we built up some machinery: the KZG Polynomial Commitment Scheme for committing to a secret polynomial and proving its evaluations, and KZG Batching in Plonk for turning “this polynomial is zero on the whole domain $H$” into a single quotient check. I promised these would pay off in a later post where we do something more complicated. This is that post.]]></summary></entry><entry><title type="html">KZG Batching in Plonk</title><link href="https://oguzhanstech.com/2025/11/07/kzg-batching-in-plonk.html" rel="alternate" type="text/html" title="KZG Batching in Plonk" /><published>2025-11-07T00:00:00+00:00</published><updated>2025-11-07T00:00:00+00:00</updated><id>https://oguzhanstech.com/2025/11/07/kzg-batching-in-plonk</id><content type="html" xml:base="https://oguzhanstech.com/2025/11/07/kzg-batching-in-plonk.html"><![CDATA[<p>In my last post, we explored the <a href="/2025/10/13/kzg-poly-commit-scheme.html">KZG Polynomial Commitment Scheme</a>, a powerful tool for proving that a secret polynomial $p(X)$ evaluates to a certain value $y$ at a public point $z$. That is, we can prove the claim $p(z) = y$.</p>

<p>This is a fantastic building block, but in larger systems like Plonk <a class="citation" href="#gabizon2019plonk">(Gabizon et al., 2019)</a>, we often need to prove more than just one evaluation. We need to prove that a polynomial equation holds true over an entire <em>set</em> of points. For example, we might need to prove $P(x) = 0$ for every single $x$ in a domain $H$.</p>

<p>How could we do this? We could run a separate KZG proof for every point in $H$, but if $H$ has thousands of elements, our proof would become enormous and the verification process painfully slow. We need a more efficient way to bundle all these checks into one.</p>

<p>The answer we are looking for is the use of KZG batching. Plonk utilizes a form of batching that’s well-suited for its constraint system, and in this post we are going to take a closer look into that approach.</p>

<p>Alternative posts that I recommend are the original paper’s Batch Opening paragraph <a class="citation" href="#kate2010constant">(Kate et al., 2010)</a> and <a href="https://dankradfeist.de/ethereum/2020/06/16/kate-polynomial-commitments.html">Dankrad Feist’s blog post’s Multiproof section</a>.</p>

<p><strong>Disclaimer:</strong> These are my personal notes and understanding of the topic. They may contain errors or oversimplifications. I don’t claim to provide absolute truth - rather, I invite discussion and corrections. If you spot any mistakes or have insights to share, please reach out!</p>

<h2 id="preliminaries">Preliminaries</h2>

<p>Some definitions and notes we’ll be using:</p>

<ul>
  <li>
    <p><strong>Polynomial Degree</strong>: $f \in \mathbb{F}_{&lt; n}[X]$ means $f$ is a polynomial with degree less than $n$.</p>
  </li>
  <li>
    <p><strong>Commitment Notation</strong>: The commitment of a polynomial $f(X)$ is represented as $[f(x)]_1$. Unless specified, commitments are in the first group, $\mathbb{G}_1$.</p>
  </li>
  <li><strong>Additive Homomorphism</strong>: Our commitment scheme is additively homomorphic. For polynomials $f(X)$, $g(X)$ and a scalar $c$:
    <ul>
      <li><strong>Addition</strong>: $[f(x)] + [g(x)] = [f(x) + g(x)]$</li>
      <li><strong>Scalar Multiplication</strong>: $c[f(x)] = [c \cdot f(x)]$</li>
    </ul>
  </li>
  <li>
    <p><strong>Pairings</strong>: $e([f]_1, [g]_2)$ is our bilinear pairing function.</p>
  </li>
  <li>
    <p><strong>Vanishing Polynomial</strong>: For a set of points $S = {s_0, s_1, \dots, s_{k-1}}$, the vanishing polynomial is defined as:
\(Z_S(X) \coloneqq \prod_{i=0}^{k-1} (X - s_i)\)
This polynomial has a crucial property: it evaluates to $0$ for every single point in the set $S$.</p>
  </li>
  <li><strong>Prerequisites</strong>: I’ll assume you’re familiar with the concepts from my <a href="/2025/10/13/kzg-poly-commit-scheme.html">previous post on KZG</a>.</li>
</ul>

<h2 id="the-core-idea-from-many-checks-to-one">The Core Idea: From Many Checks to One</h2>

<p>Let’s say we have a claim: a secret polynomial $P(X)$ is equal to $0$ on every point in a public domain $H$.</p>

<p>The key insight is this: a polynomial $P(X)$ is zero at every point $h$ in $H$ <em>if and only if</em> it is divisible by the vanishing polynomial $Z_H(X)$.</p>

<p>Why is this true? From basic algebra, we know that if $P(h) = 0$, then $(X-h)$ must be a factor of $P(X)$. If this is true for <em>all</em> $h$ in $H$, then the product of all those factors, which is exactly our vanishing polynomial $Z_H(X)$, must also be a factor of $P(X)$.</p>

<p>This means that if our claim is true, the division must be exact. We can define a quotient polynomial $Q(X)$ like this:</p>

\[Q(X) \coloneqq \dfrac{P(X)}{Z_H(X)}\]

<p>This single equation, $P(X) = Q(X) \cdot Z_H(X)$, is equivalent to the $n$ individual checks we started with! Now, our task has been reduced to proving that this new polynomial identity is true.</p>

<h2 id="the-protocol">The Protocol</h2>

<p>How can a prover convince a verifier of this identity? They can’t just send the polynomial $Q(X)$, because $P(X)$ is secret, so $Q(X)$ must also be secret.</p>

<p>Instead, we can use polynomial commitments and a random challenge point, very similar to what we did in KZG. By the Schwartz-Zippel lemma, if two polynomials are equal, they will evaluate to the same value at a random point $\zeta$ with very high probability.</p>

<p>So, the verifier will check the identity $P(\zeta) = Q(\zeta) \cdot Z_H(\zeta)$.</p>

<p><strong>Claim:</strong> A secret polynomial $P(X)$ is $0$ on the public domain $H$.</p>

<p><strong>Common Inputs:</strong> The vanishing polynomial for the domain $H$, which is $Z_H(X)$.</p>

<p><strong>Prover Inputs:</strong> The secret polynomial $P(X)$.</p>

<p><strong>Protocol:</strong></p>

<ol>
  <li>
    <p><strong>Compute the Quotient Polynomial</strong>:
The prover computes the quotient $Q(X) = \dfrac{P(X)}{Z_H(X)}$. If the claim is false, this division will have a non-zero remainder, and the protocol will fail.</p>
  </li>
  <li>
    <p><strong>Commit to Polynomials</strong>:
The prover commits to the secret polynomials $P(X)$ and $Q(X)$ and sends the commitments $[P(x)]_1$ and $[Q(x)]_1$ to the verifier.</p>
  </li>
  <li>
    <p><strong>Get Random Challenge</strong>:
The verifier generates a random challenge scalar $\zeta$ (zeta) and sends it to the prover. (In a real system, this would be derived from the transcript using Fiat-Shamir).</p>
  </li>
  <li>
    <p><strong>Prove the Evaluation</strong>:
The prover and verifier now want to check the identity $P(\zeta) - Q(\zeta) \cdot Z_H(\zeta) = 0$. This is an evaluation proof! The prover needs to prove that the remainder polynomial $R(X) \coloneqq P(X) - Z_H(\zeta) \cdot Q(X)$ evaluates to $0$ at $X = \zeta$.</p>

    <p>Notice a subtle but important detail here. In the expression for $R(X)$, we use $Z_H(\zeta)$. This is the <em>evaluation</em> of the public polynomial $Z_H(X)$ at the point $\zeta$, which is just a scalar value. We do this because we can’t multiply two commitments together, but we <em>can</em> multiply a commitment by a scalar. Later on, we need to evaluate the entire expression at $\zeta$, so putting $Z_H(X)$’s evaluation here does not cause trouble.</p>

    <p>To prove $R(\zeta) = 0$, the prover computes the KZG witness polynomial:</p>

\[W_\zeta(X) \coloneqq \dfrac{R(X)}{X-\zeta} = \dfrac{P(X) - Z_H(\zeta) \cdot Q(X)}{X-\zeta}\]

    <p>The prover commits to this witness, $[W_\zeta(x)]_1$, and sends it to the verifier.</p>
  </li>
  <li>
    <p><strong>Verifier Check</strong>:
The verifier now has all the pieces: $[P(x)]_ 1$, $[Q(x)]_ 1$, and $[W_\zeta(x)]_ 1$. They can perform the final check using a single pairing equation. The verifier wants to confirm that</p>

\[\begin{align}
W_\zeta(X) \cdot (X - \zeta) &amp;- R(X) &amp;= 0 \notag \\
W_\zeta(X) \cdot (X - \zeta) &amp;- (P(X) - Z_H(\zeta)\cdot Q(X)) &amp;= 0 \notag
\end{align}\]

    <p>and for that it uses the committed values in the following check:</p>

\[e([W_\zeta(x)]_1, [x]_2 - \zeta[1]_2) \stackrel{?}{=} e([P(x)]_1 - Z_H(\zeta)[Q(x)]_1, [1]_2)\]

    <p>Notice that this equation forces the prover to use the committed polynomial $P(X)$, the provided quotient $Q(X)$ to be the true division, and hence the right hand side effectively calculates $[R(x)]_1$.</p>

    <p>Then, the left hand side is designed so that it only holds if $R(X)$ is $0$ at $\zeta$, a random point. As we explained before, this implies the remainder polynomial is exactly $0$ everywhere, that is the division is exact, and therefore $P(X)$ is $0$ on $H$.</p>
  </li>
</ol>

<h2 id="conclusion">Conclusion</h2>

<p>We’ve just learned a powerful and efficient technique. We’ve managed to take a claim that required $n$ separate checks ($P(x) = 0$ for all $x$ in $H$) and compress it into a single, compact proof that requires only a few commitments and one pairing check.</p>

<h2 id="references">References</h2>

<ol class="bibliography"><li><span id="gabizon2019plonk">Gabizon, A., Williamson, Z. J., &amp; Ciobotaru, O. (2019). Plonk: Permutations over lagrange-bases for oecumenical noninteractive arguments of knowledge. <i>Cryptology EPrint Archive</i>.</span></li>
<li><span id="kate2010constant">Kate, A., Zaverucha, G. M., &amp; Goldberg, I. (2010). Constant-size commitments to polynomials and their applications. <i>International Conference on the Theory and Application of Cryptology and Information Security</i>, 177–194.</span></li></ol>]]></content><author><name></name></author><category term="cryptography" /><category term="zkp" /><category term="plonk" /><category term="polynomials" /><category term="kzg" /><summary type="html"><![CDATA[In my last post, we explored the KZG Polynomial Commitment Scheme, a powerful tool for proving that a secret polynomial $p(X)$ evaluates to a certain value $y$ at a public point $z$. That is, we can prove the claim $p(z) = y$.]]></summary></entry><entry><title type="html">KZG Polynomial Commitment Scheme</title><link href="https://oguzhanstech.com/2025/10/13/kzg-poly-commit-scheme.html" rel="alternate" type="text/html" title="KZG Polynomial Commitment Scheme" /><published>2025-10-13T00:00:00+00:00</published><updated>2025-10-13T00:00:00+00:00</updated><id>https://oguzhanstech.com/2025/10/13/kzg-poly-commit-scheme</id><content type="html" xml:base="https://oguzhanstech.com/2025/10/13/kzg-poly-commit-scheme.html"><![CDATA[<p>KZG polynomial commitment scheme <a class="citation" href="#kate2010constant">(Kate et al., 2010)</a> is a fundamental building block for many zero-knowledge proof systems, including Plonk <a class="citation" href="#gabizon2019plonk">(Gabizon et al., 2019)</a>. In this post, I’ll explain how KZG works and provide the groundwork needed for understanding more complex protocols.</p>

<p>My goal is making these posts implementation oriented - not too abstract. However, some used concepts will not be described in full, this will be more like <em>some notes on cryptography</em>.</p>

<p>For more complete, reliable, and more reviewed text you may refer to <a href="https://dankradfeist.de/ethereum/2020/06/16/kate-polynomial-commitments.html">Dankrad Feist’s blog</a> or to the original paper <a class="citation" href="#kate2010constant">(Kate et al., 2010)</a>.</p>

<p><strong>Disclaimer:</strong> These are my personal notes and understanding of the topic. They may contain errors or oversimplifications. I don’t claim to provide absolute truth - rather, I invite discussion and corrections. If you spot any mistakes or have insights to share, please reach out!</p>

<h2 id="preliminaries">Preliminaries</h2>

<p>Some definitions and notes:</p>

<p><strong>Polynomial Degree</strong>: $f \in \mathbb{F}_{&lt; n}[X]$ means $f$ is a polynomial with degree less than $n$.</p>

<p><strong>Commitment Notation</strong>: The commitment of polynomial $f(X)$ is represented as $[f(x)]_1$ if it is in the first group, and $[f(x)]_2$ if it is in the second group. If no subscript is provided, it is a commitment in the first group.</p>

<p><strong>Additive Homomorphism</strong>: The commitment scheme allows for addition and scalar multiplication. This means that for polynomials $f(X)$ and $g(X)$ and a scalar $c$:</p>
<ul>
  <li><strong>Addition</strong>: $[f(x)] + [g(x)] = [f(x) + g(x)]$</li>
  <li><strong>Scalar Multiplication</strong>: $c[f(x)] = [c \cdot f(x)]$</li>
</ul>

<p><strong>Pairings</strong>: $e([f]_1, [g]_2)$ takes two arguments and is bilinear:</p>

\[e(a[f]_1, b[g]_2) = e([f]_1, ab[g]_2) = e(ab[f]_1, [g]_2) = e([f]_1, [g]_2)^{ab}\]

<h2 id="what-is-kzg">What is KZG?</h2>

<p>KZG is the polynomial commitment scheme (PCS) used by Plonk, and the PCS I chose to use for my work.
A polynomial commitment scheme is a protocol that allows the prover to commit to a polynomial, and then in zero-knowledge prove its evaluation at a given point.</p>

<p>The high level idea of KZG is that</p>

<ul>
  <li>If a polynomial $p(X)$ passes through $(x_0, y_0)$, then the polynomial $p’(X) \coloneqq p(X)-y_0$ is $0$ at $X=x_0$.</li>
  <li>Then $p’(X)$ has a root at $X=x_0$. That is, $X-x_0$ is a factor of it and if we divide $p’(X)$ by this value, the division must be exact.</li>
  <li>If we can prove that the division is exact, for example by multiplying the quotient with the divisor and checking if the result is the same as the expected dividend, we would be proving the polynomial evaluation.</li>
  <li>That multiplication can be proven using elliptic curves and pairings. That is what KZG does.</li>
</ul>

<p>In the following section, I explain this by showing each step.</p>

<h2 id="the-protocol">The Protocol</h2>

<p><strong>Claim:</strong> $p(X) \in \mathbb{F}_{&lt; n}[X]$ is a private polynomial and it passes through the public point $(x_0, y_0)$.</p>

<p><strong>Setup:</strong> Trusted setup provides the commitments $[1]_1, [x]_1, [x^2]_1, \dots, [x^{n-1}]_1$ and $[1]_2, [x]_2$. This is called the Structured Reference String, or SRS for short.</p>

<p>Here, $x$ is a toxic variable that no one should know. It is required for the generation of SRS, but then it must be destroyed. Because if someone knows $x$, they can make false proofs, i.e. showing a polynomial passes through a point when it actually does not.</p>

<p><strong>Common Inputs:</strong> $(x_0, y_0) \in \mathbb{F}^2$</p>

<p><strong>Prover Inputs:</strong> $p(X) \in \mathbb{F}_ {&lt; n}[X]$, written as $p(X) = c_ 0 + c_ 1 X + c_ 2 X^2 + \dots + c_{n-1} X^{n-1}$</p>

<p><strong>Protocol:</strong></p>

<ol>
  <li>
    <p><strong>Compute the Quotient Polynomial</strong>:</p>

    <p>Prover computes the quotient polynomial $q(X) \coloneqq \dfrac{p(X) - y_0}{X-x_0}$</p>

    <p>Notice that for this to be true, there should be no remainder polynomial. This is true if the claim $p(x_0) \stackrel{?}{=} y_0$ is true. To see this, assume the claim. Then observe the polynomial $p(X) - y_0$ has a root at $X=x_0$:</p>

\[\begin{align}
 (p(X) - y_0)\mid_{X=x_0} &amp;= p(x_0) - y_0 \notag \\
 p(x_0) - y_0 &amp;= y_0 - y_0 = 0 \notag \\
 \end{align}\]

    <p>Therefore, $X-x_0$ is a factor of it and thus the division is exact, i.e. the remainder polynomial of the division is $0$.</p>
  </li>
  <li>
    <p><strong>Commit to Polynomials</strong>:</p>

    <p>Prover commits to $p(X), q(X)$, that is using SRS it computes</p>

\[\begin{align}
 [p(x)]_1 &amp;= c_0[1]_1 + c_1[x]_1 + \dots + c_{n-1}[x^{n-1}]_1 \notag \\
 [q(x)]_1 &amp;= c^q_0[1]_1 + c^q_1[x]_1 + \dots + c^q_{m-1}[x^{m-1}]_1 \notag \\
 \end{align}\]

    <p>This computation is possible due to the following facts: commitments to the powers of $x$, i.e. $[x^j]_1$, are present in the SRS, and the commitment scheme is additively homomorphic. Therefore, we can compute the scalar multiplications, e.g. $c_1[x]_1$, and then sum these scaled commitments together. This is called <strong>multiscalar multiplication (MSM)</strong>.</p>

\[\begin{align}
 [p(x)]_1 = \sum_{i=0}^{n-1} c_i[x^i]_1 \tag{$n-1$ is the degree of $p(X)$}
 \end{align}\]

    <p>Notice that the commitment is just evaluating the polynomial at $X=x$, the toxic waste, and taking that to the elliptic curve.</p>
  </li>
  <li>
    <p><strong>Send Commitments</strong>:</p>

    <p>Prover sends $[p(x)]_1, [q(x)]_1$ to the verifier.</p>
  </li>
  <li>
    <p><strong>Verifier Check</strong>:</p>

    <p>Verifier checks the equation:</p>

\[\begin{align}
 e([q(x)]_1, [x]_2 + (-x_0)[1]_2) &amp;\stackrel{?}{=} e([p(x)]_1 + (-y_0)[1]_1,[1]_2) \notag \\
 \end{align}\]

    <p>If the check passes, the verifier accepts the claim $p(x_0) \stackrel{?}{=} y_0$.</p>
  </li>
</ol>

<h2 id="understanding-the-pairing-equation">Understanding the Pairing Equation</h2>

<p>The pairing allows us to represent the multiplication of two commitments, the divisor and the quotient as a target group element (the output of the pairing). That is, we reconstructed the dividend as a target group element on the left hand side. Then, we put the expected dividend on the right hand side. Finally, we checked if the two pairings are equivalent.</p>

<p>The important fact here is that we could not simply multiply the quotient and the divisor commitments because we only have addition. The pairing gave us a workaround for this: representing that multiplication as its target group’s element.</p>

<p>In other words, the pairing helped us revert the division to a multiplication and check its result.</p>

<p><strong>Reading a pairing equation:</strong> Apply the following transformations:</p>

<ul>
  <li>Every commitment is an evaluation at $x$, the toxic variable. Revert it by replacing $x$ with $X$.</li>
  <li>$e(a, b)$ is just $a\cdot b$.</li>
</ul>

<p>Therefore, if we apply them to the pairing equation, we get</p>

\[\begin{align}
q(X)\cdot(X-x_0) \stackrel{?}{=} p(X) - y_0 \notag
\end{align}\]

<p>This is <em>a conceptual transformation</em> I use for ease of reading. Some information is lost during this transformation and hence it does not perfectly represent the pairing. Alternatively, Dankrad <a class="citation" href="#feist2020kate">(Feist, 2020)</a> writes this pairing in the target group:</p>

\[\begin{align}
[q(x)\cdot(x-x_0)]_T \stackrel{?}{=} [p(x) - y_0]_T \notag
\end{align}\]

<h2 id="conclusion">Conclusion</h2>

<p>We have seen fundamental concepts like how to compute a commitment to a polynomial, how additive homomorphism works, how pairings work, and how KZG’s protocol is designed. We will build on these in the later posts where we work on more complicated ideas.</p>

<h2 id="references">References</h2>

<ol class="bibliography"><li><span id="kate2010constant">Kate, A., Zaverucha, G. M., &amp; Goldberg, I. (2010). Constant-size commitments to polynomials and their applications. <i>International Conference on the Theory and Application of Cryptology and Information Security</i>, 177–194.</span></li>
<li><span id="gabizon2019plonk">Gabizon, A., Williamson, Z. J., &amp; Ciobotaru, O. (2019). Plonk: Permutations over lagrange-bases for oecumenical noninteractive arguments of knowledge. <i>Cryptology EPrint Archive</i>.</span></li>
<li><span id="feist2020kate">Feist, D. (2020). <i>KZG polynomial commitments</i>. Dankrad Feist’s blog. https://dankradfeist.de/ethereum/2020/06/16/kate-polynomial-commitments.html</span></li></ol>]]></content><author><name></name></author><category term="cryptography" /><category term="zkp" /><category term="kzg" /><category term="commitment schemes" /><summary type="html"><![CDATA[KZG polynomial commitment scheme (Kate et al., 2010) is a fundamental building block for many zero-knowledge proof systems, including Plonk (Gabizon et al., 2019). In this post, I’ll explain how KZG works and provide the groundwork needed for understanding more complex protocols.]]></summary></entry><entry><title type="html">SSL Pinning Bypass: Network Security Config</title><link href="https://oguzhanstech.com/2025/08/25/ssl-pinning-bypass-network-security-config.html" rel="alternate" type="text/html" title="SSL Pinning Bypass: Network Security Config" /><published>2025-08-25T00:00:00+00:00</published><updated>2025-08-25T00:00:00+00:00</updated><id>https://oguzhanstech.com/2025/08/25/ssl-pinning-bypass-network-security-config</id><content type="html" xml:base="https://oguzhanstech.com/2025/08/25/ssl-pinning-bypass-network-security-config.html"><![CDATA[<p>In this post, I will talk about how I bypassed the simplest SSL pinning method: <a href="https://developer.android.com/privacy-and-security/security-config">Network Security Config</a> via APK resource editing. I will also explain my setup.</p>

<p>I assume you know how to use the tools mitmproxy, an Android proxy client, apktool, adb and do not detail it here.</p>

<h2 id="why-do-this-at-all">Why Do This At All?</h2>
<p>SSL Pinning protects your apps their network protocols revealed. Though it is a client side protection and you should focus on server side for the most part, it is still relevant: This will slow down anyone that wants to learn your API, bot it or cheat it.</p>

<p>Studying SSL pinning bypasses is both a fun technical challenge and a way to better understand app defenses. My personal goal here is to observe app traffic directly in mitmproxy, while learning how different protections work.</p>

<h2 id="bypassing-network-security-config">Bypassing Network Security Config</h2>
<p>Bypassing Network Security Config was simple:</p>
<ul>
  <li>Decompile the APK with <a href="https://apktool.org/">apktool</a></li>
  <li>Find the name of the config from <code class="language-plaintext highlighter-rouge">AndroidManifest.xml</code> in the <code class="language-plaintext highlighter-rouge">application</code> tag as the value of <code class="language-plaintext highlighter-rouge">android:networkSecurityConfig</code>, usually it is <code class="language-plaintext highlighter-rouge">res/xml/network_security_config.xml</code></li>
  <li>then set it to be:</li>
</ul>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span>
<span class="nt">&lt;network-security-config&gt;</span>
    <span class="nt">&lt;base-config</span> <span class="na">cleartextTrafficPermitted=</span><span class="s">"false"</span><span class="nt">&gt;</span>
        <span class="nt">&lt;trust-anchors&gt;</span>
            <span class="nt">&lt;certificates</span> <span class="na">src=</span><span class="s">"system"</span> <span class="nt">/&gt;</span>
            <span class="nt">&lt;certificates</span> <span class="na">src=</span><span class="s">"user"</span> <span class="nt">/&gt;</span>
        <span class="nt">&lt;/trust-anchors&gt;</span>
    <span class="nt">&lt;/base-config&gt;</span>
<span class="nt">&lt;/network-security-config&gt;</span>
</code></pre></div></div>
<p>This will make it trust installed certificates - including the <a href="https://www.mitmproxy.org/">mitmproxy</a>’s certificate if installed. Also if there was any certificate pinned this way, we effectively erased it.<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup></p>

<p>We can see that the default setting is to only trust system certificates and no cleartext traffic, for API level 28 and higher according to <a href="https://developer.android.com/privacy-and-security/security-config">here</a>. Therefore, adding user certificates as a trust anchor is necessary.</p>

<p>After this, we can build and sign the app and install it on the target device. Next, we need to put the app’s network traffic through mitmproxy to observe it.</p>

<h2 id="setting-up-the-proxy">Setting up the Proxy</h2>
<p>We need to run mitmproxy on the host and configure the target device to use it. However, the host and the target device possibly lie on different networks and cannot see each other. We cannot make the target device connect to mitmproxy then. I assume a USB connection to the target device is present, so I will use adb to make the devices connect. In the next subsection I explain how.</p>

<h3 id="a-note-on-adb-port-forwarding">A Note on ADB Port Forwarding</h3>
<p>ADB has two useful commands for port forwarding, the second one is something I wish I had seen sooner<sup id="fnref:2"><a href="#fn:2" class="footnote" rel="footnote" role="doc-noteref">2</a></sup>. Here I detail them:</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adb forward tcp:7777 tcp:8888
</code></pre></div></div>
<p>This forwards the port 7777 on the host (i.e. the device running adb, your computer) to the port 8888 on the target device (i.e. your mobile which is connected using a USB cable maybe). What this means is, any connection to port 7777 on your host will be directed to the port 8888 on your target device. For example, if you have an HTTP server listening on port 8888 on your target device, you can access it on your computer via <code class="language-plaintext highlighter-rouge">localhost:7777</code>.</p>

<p>The other command I wish I had learned sooner is the <code class="language-plaintext highlighter-rouge">adb reverse</code> command.</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adb reverse tcp:3333 tcp:4444
</code></pre></div></div>
<p>This does the opposite of the <code class="language-plaintext highlighter-rouge">adb forward</code> command as the name suggests: The port 3333 on the target device is forwarded to the port 4444 on the host device. That is, if you host the HTTP server on your host at 4444, you can access it on your mobile via <code class="language-plaintext highlighter-rouge">localhost:3333</code>.</p>

<p>Mitmproxy listens at 8080 by default, so we are going to do</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adb reverse tcp:8080 tcp:8080
</code></pre></div></div>
<p>Now we can run a http proxy app on the target device and connect it to <code class="language-plaintext highlighter-rouge">127.0.0.1:8080</code> so that it connects to the mitmproxy. Then you should be able to see the network traffic.</p>

<h2 id="conclusion">Conclusion</h2>
<p>We saw how easy it is to bypass the Network Security Config. All we needed to do was decompile, change an XML file, and recompile. To defend against modification of the APK, one can harden their APK using special tools that make apktool fail.</p>

<p><strong>Disclaimer:</strong> This post is intended solely for educational and research purposes. The techniques described here should only be applied to applications you own or have explicit permission to analyze. Reverse engineering, modifying, or intercepting traffic of third-party apps without consent may violate their terms of service and applicable laws. I do not encourage or condone using these methods against third-party apps without permission.</p>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:1">
      <p>Actually I used a script for this and it did not remove the domain-configs and pin-sets, it just added this base-config after them. I thought bypass would fail because pin-set tags would override the base-config, yet it works. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:2">
      <p>https://android.googlesource.com/platform/packages/modules/adb/+/refs/heads/master/docs/user/adb.1.md <a href="#fnref:2" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name></name></author><category term="android" /><category term="security" /><category term="ssl-pinning" /><category term="mitmproxy" /><summary type="html"><![CDATA[In this post, I will talk about how I bypassed the simplest SSL pinning method: Network Security Config via APK resource editing. I will also explain my setup.]]></summary></entry><entry><title type="html">SSL Pinning Bypass: SSLContext</title><link href="https://oguzhanstech.com/2025/08/25/ssl-pinning-bypass-sslcontext.html" rel="alternate" type="text/html" title="SSL Pinning Bypass: SSLContext" /><published>2025-08-25T00:00:00+00:00</published><updated>2025-08-25T00:00:00+00:00</updated><id>https://oguzhanstech.com/2025/08/25/ssl-pinning-bypass-sslcontext</id><content type="html" xml:base="https://oguzhanstech.com/2025/08/25/ssl-pinning-bypass-sslcontext.html"><![CDATA[<p>In this post, I will explain how to bypass SSL Pinning via <a href="https://developer.android.com/reference/javax/net/ssl/SSLContext">SSLContext</a> using <a href="https://frida.re/">Frida</a>.</p>

<p>As the injection method, I use dylib injection for the reasons I explain below.</p>

<h2 id="split-apks">Split APKs</h2>
<p>Nowadays most apps are split APKs, meaning that they are split into different parts: A main APK that contains .dex files and most resources, one for native libraries (config.arm64_v8a.apk), one for each language the app supports (config.en.apk), one for graphics (config.mdpi.apk) etc. Furthermore, sometimes apktool fails on decompiling/recompiling the main APK and in such cases dylib injection is useful. This is because we do not have to change main APK at all (just resign it with our key), we only have to inject the Frida gadget into a library which lies in <code class="language-plaintext highlighter-rouge">config.&lt;arch&gt;.apk</code>.</p>

<h2 id="injecting-frida-into-a-library">Injecting Frida into a library</h2>
<p>First, we decompile <code class="language-plaintext highlighter-rouge">config.&lt;arch&gt;.apk</code> that contains the libraries. Then we will find a library that is loaded first, like <code class="language-plaintext highlighter-rouge">libreact_render_core.so</code> or anything else that might work. Then we will use the following script to inject the Frida gadget <code class="language-plaintext highlighter-rouge">libfrida-gadget.so</code> into it.</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">lief</span><span class="p">,</span> <span class="n">sys</span>

<span class="n">target</span> <span class="o">=</span> <span class="n">sys</span><span class="p">.</span><span class="n">argv</span>
<span class="n">libnative</span> <span class="o">=</span> <span class="n">lief</span><span class="p">.</span><span class="nf">parse</span><span class="p">(</span><span class="n">target</span><span class="p">)</span>
<span class="n">libnative</span><span class="p">.</span><span class="nf">add_library</span><span class="p">(</span><span class="sh">"</span><span class="s">libfrida-gadget.so</span><span class="sh">"</span><span class="p">)</span>
<span class="n">libnative</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="n">target</span><span class="p">)</span>
</code></pre></div></div>
<p>This adds <code class="language-plaintext highlighter-rouge">libfrida-gadget.so</code> to the ELF’s dynamic section so that our gadget is loaded alongside the target library<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>. You can check this with the <code class="language-plaintext highlighter-rouge">readelf</code> command:</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>readelf <span class="nt">-d</span> libreact_render_core.so 

Dynamic section at offset 0x22000 contains 43 entries:
  Tag        Type                         Name/Value
 0x00000001 <span class="o">(</span>NEEDED<span class="o">)</span>                     Shared library: <span class="o">[</span>libfrida.so]
 0x00000001 <span class="o">(</span>NEEDED<span class="o">)</span>                     Shared library: <span class="o">[</span>libjsi.so]
 0x00000001 <span class="o">(</span>NEEDED<span class="o">)</span>                     Shared library: <span class="o">[</span>liblogger.so]
 0x00000001 <span class="o">(</span>NEEDED<span class="o">)</span>                     Shared library: <span class="o">[</span>libreact_config.so]
...
</code></pre></div></div>
<p>This is explained in a much better detail at <a href="https://lief.re/doc/stable/tutorials/09_frida_lief.html">here</a>.</p>

<p>Once the Frida gadget works (the app freezes until I connect to it on startup), I ran the following command, <a href="/2025/08/25/ssl-pinning-bypass-network-security-config.html#a-note-on-adb-port-forwarding">explained more detailed here</a>:</p>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adb forward tcp:27042 tcp:27042
</code></pre></div></div>
<p>This forwards the port 27042 on my computer to the same port on my mobile, where Frida gadget listens by default. I can then connect to the Frida gadget via <code class="language-plaintext highlighter-rouge">frida -R gadget -l ssl.js</code> and let the script do the bypassing.</p>

<h3 id="frida---ssl-pinning-bypass-script">Frida - SSL Pinning Bypass Script</h3>
<p>The <code class="language-plaintext highlighter-rouge">ssl.js</code> I used above can be found <a href="https://codeshare.frida.re/@pcipolloni/universal-android-ssl-pinning-bypass-with-frida/">here</a>. This script does the following:</p>
<ul>
  <li>Get <code class="language-plaintext highlighter-rouge">javax.net.ssl.TrustManagerFactory</code> and use it to create a TrustManager for the certificate at <code class="language-plaintext highlighter-rouge">/data/local/tmp/cert-der.crt</code><sup id="fnref:2"><a href="#fn:2" class="footnote" rel="footnote" role="doc-noteref">2</a></sup></li>
  <li>Hook <code class="language-plaintext highlighter-rouge">SSLContext.init</code> method so that all calls to it are intercepted.</li>
  <li>When <code class="language-plaintext highlighter-rouge">SSLContext.init</code> is called, instead of the provided TrustManager, use our own that carries mitmproxy’s certificate.
Once injected, the script logs its actions in real-time, so you can confirm whether the bypass worked.</li>
</ul>

<p>And that’s it. At this point I am supposed to be able to see the traffic. However I noticed while I can see some, there is still some traffic which I cannot as evident by mitmproxy’s logs. Then I realized there was another SSL pinning layer I hadn’t addressed: Network Security Config.</p>

<h2 id="conclusion">Conclusion</h2>
<p>In this post, I demonstrated a less known way of injecting Frida by modifying shared libraries, which avoids the common pitfalls of smali recompilation errors. Then we used a Frida script to hook <code class="language-plaintext highlighter-rouge">SSLContext.init</code> method. I have no practical defense solutions for this attack - Android does not seem to provide a way to see whether SSLContext is initialized with the intended trust manager or not. For a more general defense, detecting whether the gadget is present or not can be used, perhaps by listing the libraries.</p>

<p><strong>Disclaimer:</strong> This post is intended solely for educational and research purposes. The techniques described here should only be applied to applications you own or have explicit permission to analyze. Reverse engineering, modifying, or intercepting traffic of third-party apps without consent may violate their terms of service and applicable laws. I do not encourage or condone using these methods against third-party apps without permission.</p>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:1">
      <p>Sometimes this method silently fails and I haven’t figured out why yet. Injecting the Frida gadget into another library often works. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:2">
      <p>This will be the mitmproxy’s certificate and we will push it to the device with <code class="language-plaintext highlighter-rouge">adb push mitmproxy.cert /data/local/tmp/cert-der.crt</code>. This is a directory all apps can access and that’s why we are using it. You can find mitmproxy certificates on your computer at <code class="language-plaintext highlighter-rouge">~/.mitmproxy</code> or your mobile that uses mitmproxy as proxy at <code class="language-plaintext highlighter-rouge">http://mitm.it</code>. <a href="#fnref:2" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name></name></author><category term="android" /><category term="security" /><category term="ssl-pinning" /><category term="frida" /><summary type="html"><![CDATA[In this post, I will explain how to bypass SSL Pinning via SSLContext using Frida.]]></summary></entry></feed>